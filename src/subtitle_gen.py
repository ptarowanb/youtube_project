from __future__ import annotations

from pathlib import Path


def extract_timestamps(audio_path: Path) -> list[dict]:
    raise NotImplementedError("Subtitle timestamp extraction is planned for a later phase.")
